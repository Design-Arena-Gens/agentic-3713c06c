import AudioHero from '@/components/AudioHero';

export default function Page() {
  return (
    <main className="relative min-h-screen">
      <AudioHero />
      <footer className="mt-8 pb-10 text-center text-xs text-white/50">
        High-end wired studio headphones ? FLAC ? Immersive sound
      </footer>
    </main>
  );
}
